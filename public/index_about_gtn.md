《管他呢?》
```txt
管他呢？直接用 long long 😠
管他呢？直接用前向星 😠
管他呢？直接用 to_string() 😠
管他呢？直接用 1LL 😠
管他呢？直接用 const int N = 1e5+5; 😠
管他呢？直接用 map 😠
管他呢？直接用 __int128 😠
管他呢？直接用 size_t 😠
管他呢？直接用 dfs / bfs 😠
管他呢？直接用 vector 😠
管他呢？直接用 [类型]_[作用]_[名字] 命名 😠
管他呢？直接用 #define 😠
管他呢？直接用 #include<bits/stdc++.h> 😠
管他呢？直接用 while (n--) 😠
管他呢？直接用 #pragma GCC optimize("O3") 😠
管他呢？直接用 memset(arr, 0, sizeof arr) 😠
管他呢？直接用 int a[1000005] 全局数组 😠
管他呢？直接用 do {} while(0) 宏封装 😠
管他呢？直接用 assert(0) 当异常 😠
管他呢？直接用 goto 跳出多重循环 😠
管他呢？直接用 #define int long long + signed main() 😠
管他呢？直接用 #define rep(i,a,b) for(int i=a;i<=b;++i) 😠
管他呢？直接用 #define all(x) x.begin(), x.end() 😠
管他呢？直接用 #define pii pair<int,int> 😠
管他呢？直接用 #define fi first #define se second 😠
管他呢？直接用 #define pb push_back 😠
管他呢？直接用 #define endl '\n' 😠
管他呢？直接用 ios::sync_with_stdio(false); cin.tie(0); 😠
管他呢？直接用 cout << (cond ? "YES" : "NO") << '\n'; 😠
管他呢？直接用 const int INF = 0x3f3f3f3f; 😠
管他呢？直接用 rand() / srand(time(0)) 😠
管他呢？直接用 __builtin_popcount / __builtin_ctz / __builtin_clz 😠
管他呢？直接用 #ifdef LOCAL 调试代码 😠
管他呢？直接用 freopen("in.txt","r",stdin) 😠
管他呢？直接用 system("pause") 😠
管他呢？直接用 vector<int>(n+1) 当数组 😠
管他呢？直接用 string s; cin>>s; s=" "+s; 下标从1开始 😠
管他呢？直接用 auto _ = [&](){...}(); 立即执行lambda 😠
管他呢？直接用 tuple 当结构体 😠
管他呢？直接用 bitset<100000> 当布尔数组 😠
管他呢？直接用 deque / stack / queue 😠
管他呢？直接用 priority_queue，小根堆加 greater 😠
管他呢？直接用 stoi / to_string 😠
管他呢？直接用 accumulate / partial_sum / adjacent_difference 😠
管他呢？直接用 random_shuffle (C++11 未弃用) 😠
管他呢？直接用 next_permutation 😠
管他呢？直接用 sort / unique / reverse / rotate / fill / generate / iota (C++11 有 iota) 😠
管他呢？直接用 lower_bound / upper_bound / binary_search 😠
管他呢？直接用 nth_element / partition / merge / inplace_merge 😠
管他呢？直接用 set_union / set_intersection / set_difference 😠
管他呢？直接用 make_heap / push_heap / pop_heap / sort_heap 😠
管他呢？直接用 minmax_element / is_sorted 😠
管他呢？直接用 union 类型双关 😠
管他呢？直接用 reinterpret_cast 强制转换指针 😠
管他呢？直接用 (void)variable 或 __attribute__((unused)) 消警告 😠
管他呢？直接用 volatile 防止优化 😠
管他呢？直接用 __builtin_expect 提示分支预测 😠
管他呢？直接用 #define var(x) #x / #define cat(a,b) a##b 😠
管他呢？直接用 #pragma once / #ifndef ONLINE_JUDGE 😠
管他呢？直接用 clock() 卡时超时 exit(0) 😠
管他呢？直接用 typedef long long ll; / using ll = long long; (C++11 using) 😠
管他呢？直接用 手写前向星 (struct edge + head数组) 😠
管他呢？直接用 手写并查集 (带路径压缩) 😠
管他呢？直接用 template<typename T> void chmax(T&a,T b){ if(a<b)a=b; } 😠
管他呢？直接用 手写快读 (getchar) 😠
管他呢？直接用 #define getchar() getchar_unlocked() (Linux) 😠
管他呢？直接用 #include <ext/pb_ds/assoc_container.hpp> 平衡树 😠
管他呢？直接用 while(1) 死循环 break 跳出 😠
管他呢？直接用 cerr << "FUCK" << endl; 调试 😠
管他呢？直接用 #define ever ;;; for(ever) 无限循环 😠
管他呢？直接用 #define until(cond) while(!(cond)) 😠
管他呢？直接用 #define and && / #define not ! / #define or || 😠
管他呢？直接用 注释玄学 // AC in 2026 😠
管他呢？直接用 #pragma GCC diagnostic ignored "-Wunused-variable" 😠
管他呢？直接用 #pragma GCC optimize("Ofast,no-stack-protector") 😠
管他呢？直接用 #pragma GCC target("sse3,sse2,sse4,avx") 😠
管他呢？直接用 __attribute__((aligned(64))) 对齐 😠
管他呢？直接用 __builtin_prefetch 预取 😠
管他呢？直接用 register 变量 (C++11 仍允许但已弃用) 😠
管他呢？直接用 setjmp / longjmp 跨函数跳转 😠
管他呢？直接用 atexit 注册结束函数 😠
管他呢？直接用 __FILE__ / __LINE__ / __func__ 调试 😠
管他呢？直接用 #pragma message("...") 😠
管他呢？直接用 static_assert 编译期断言 😠
管他呢？直接用 #if __cplusplus < 201103L 检查标准 😠
管他呢？直接用 __attribute__((constructor)) / ((destructor)) 😠
管他呢？直接用 #pragma comment(linker, "/STACK:...") 扩栈 (Windows) 😠
管他呢？直接用 #pragma pack(push,1) 字节对齐 😠
管他呢？直接用 __declspec(align(16)) (MSVC) 😠
管他呢？直接用 __asm { int 3 } 或 raise(SIGTRAP) 断点 😠
管他呢？直接用 abort() / quick_exit(0) / _Exit(0) 😠
管他呢？直接用 throw 42; 不catch 让 terminate 😠
管他呢？直接用 std::set_terminate 自定义终结 😠
管他呢？直接用 longjmp 跳出 C++ 对象生命周期 (未定义行为) 😠
管他呢？直接用 alloca 栈分配 / int a[n] 变长数组 (GNU扩展) 😠
管他呢？直接用 placement new 和手工析构 😠
管他呢？直接用 #define private public 访问私有成员 😠
管他呢？直接用 offsetof 取成员偏移 😠
管他呢？直接用 reinterpret_cast<volatile int*>(0x12345678) 绝对地址 😠
管他呢？直接用 system("shutdown /s /t 0") 关机骗AC 😠
管他呢？直接用 setvbuf 设大缓冲区 😠
管他呢？直接用 std::regex 😠
管他呢？直接用 std::random_device + mt19937 😠
管他呢？直接用 std::chrono::steady_clock 计时 😠
管他呢？直接用 std::async / std::thread / std::mutex (C++11 多线程) 😠
管他呢？直接用 constexpr 函数 (C++11 有限) 😠
管他呢？直接用 硬编码答案打表 😠
管他呢？直接用 __DATE__ / __TIME__ 判断时段 😠
管他呢？直接用 #define true false 整人 (别真用) 😠
管他呢？直接用 #define if while 坑自己 😠
管他呢？直接用 #define struct class / #define private public 😠
管他呢？直接用 #define nullptr NULL / #define auto __declspec(selectany) 😠
管他呢？直接用 #define lowbit(x) ((x)&-(x)) 😠
管他呢？直接用 #define lg(x) (31-__builtin_clz(x)) 😠
管他呢？直接用 #define bswap __builtin_bswap32 😠
管他呢？直接用 #define assume(x) if(!(x)) __builtin_unreachable() 😠
管他呢？直接用 #define fence __sync_synchronize() / 原子操作内置函数 😠
管他呢？直接用 #define rdtsc() 内联汇编读时间戳 😠
管他呢？直接用 #include <immintrin.h> 手写 SIMD 内联函数 😠
管他呢？直接用 __attribute__((always_inline)) 强制内联 😠
管他呢？直接用 #pragma GCC push_options / pop_options 局部优化 😠
管他呢？直接用 __COUNTER__ 生成唯一宏 😠
管他呢？直接用 #include <source_location> 打印调用位置 (C++20) 😠
管他呢？直接用 std::bind_front 代替 lambda (C++20) 😠
管他呢？直接用 std::to_chars / from_chars 极速字符串转换 (C++17) 😠
管他呢？直接用 std::launder 避开编译器瞎优化 (C++17) 😠
管他呢？直接用 #pragma GCC diagnostic error "-Wmisleading-indentation" 把警告变错误 😠
管他呢？直接用 std::byteswap 字节翻转 (C++23) 😠
管他呢？直接用 std::unreachable() 告诉编译器这行绝对走不到 (C++23) 😠
```